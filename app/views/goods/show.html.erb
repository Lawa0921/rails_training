<h1>單個商品</h1>
<%= @good.name %>
<%= @good.description %>
<%= @good.price %>
<%= @good.discount %>
<%= @good.public %>
<%= @good.status %>
<% if @good.photos.attached?%>
  <% @good.photos.each do |photo|%>
    <%= image_tag photo.variant(resize_to_limit: [200, 200])%>
    <%= link_to "刪除圖片", remove_photo_good_path(photo_id: photo), data: {method: "DELETE", confirm: "確定要刪除圖片嗎？"} %>
  <% end %>
<% end %>

<%= link_to "回到商品列表", goods_path %>
